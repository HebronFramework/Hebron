public unsafe class $arrayTypeName$: UnsafeArray1D<$typeName$>
{
	public $arrayTypeName$(int size): base(size, sizeof($typeName$))
	{
	}

	public $arrayTypeName$($typeName$[] data): base(data, sizeof($typeName$))
	{
	}

	public static implicit operator $typeName$ *($arrayTypeName$ array)
	{
		return ($typeName$*)array.ToPointer();
	}

	public static $typeName$* operator +($arrayTypeName$ array, int delta)
	{
		return ($typeName$*)array.ToPointer() + delta;
	}
}